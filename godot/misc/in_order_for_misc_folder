There wasn't a file at the top level of the misc folder in Godot, but a file is necessary here because of GitHub's limitation on the number of files you can upload at once, and the total number of files in the misc folder is bigger than 100, so I can't just upload the whole folder.
